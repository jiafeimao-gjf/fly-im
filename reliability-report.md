# Fly IM 系统可靠性分析报告

> 分析日期：2026-03-29
> 分析范围：fly-im-server (Python FastAPI WebSocket IM Server) + fly-h5-client

---

## 1. 消息可靠性

### 1.1 消息发送失败的重试机制

**问题描述：**
- 消息流程：`db.commit()` → 推送给接收者 → 发送 ACK 给发送者
- 如果 `commit()` 成功后、接收者收到消息前进程崩溃或网络中断，发送者会收到 ACK 但接收者实际未收到
- 完全没有重试机制

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 引入消息状态表（待发送/已发送/已送达），客户端收到消息后返回 `delivered` 确认
- 服务端定期扫描未确认消息进行重试

---

### 1.2 ACK 超时处理

**问题描述：**
- 服务端没有实现 ACK 超时逻辑
- 客户端发送消息后，如果网络中断，客户端需要自己处理超时重试
- 服务端完全不验证 ACK

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 服务端记录消息发送时间，启动超时计时器
- 超时后重试发送

---

### 1.3 消息持久化失败的处理

**问题描述：**
```python
db.add(message)
db.commit()
```
- 如果 `commit()` 失败，异常被捕获但仅打印日志，发送者仍会收到错误 ACK

**风险等级：中**

**现有缓解措施：** 无

**建议修复方案：**
- 捕获数据库异常，返回明确的错误消息给客户端

---

### 1.4 重复消息的检测和处理

**问题描述：**
- `id` 是主键，重复 ID 会被数据库拒绝
- 客户端可以自己实现去重

**风险等级：低**

**现有缓解措施：** 主键约束

---

## 2. 连接可靠性

### 2.1 WebSocket 断线重连

**问题描述：**
- 服务端完全不参与重连逻辑
- 客户端断线后，服务端 `manager.disconnect` 会清理连接
- 客户端需要完全自己实现重连策略
- 服务端不提供会话恢复机制

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 服务端支持"恢复"会话，恢复未完成的消息发送
- 或消息持久化 + 客户端拉取未读消息

---

### 2.2 心跳保活机制

**问题描述：**
- 每30秒发送 `ping`
- 收到 `pong` 后不做任何验证（只执行 `pass`）
- `WS_PING_TIMEOUT = 10` 定义了但**从未使用**，无法检测对端是否真的存活

**风险等级：中**

**现有缓解措施：** 有 ping 机制（但不验证）

**建议修复方案：**
- 发送 ping 后启动计时器，规定时间内未收到 pong 则断开连接

---

### 2.3 恶意断连处理

**问题描述：**
- `WebSocketDisconnect` 被静默捕获
- 没有记录断连事件
- 没有防御措施

**风险等级：中**

**现有缓解措施：** 无

---

### 2.4 多设备同时在线

**问题描述：**
- 当前实现只支持单个 WebSocket 连接（`active_connections: dict[str, WebSocket]`）
- 同一个用户多设备登录，后一个会覆盖前一个连接

**风险等级：中**

**现有缓解措施：** 无

**建议修复方案：**
- 支持单个用户多个 WebSocket 连接（`dict[str, list[WebSocket]]`）
- 消息推送给所有设备

---

## 3. 数据一致性

### 3.1 数据库写入失败

**问题描述：**
- SQLite 使用文件锁，并发写入时会导致 `database is locked` 错误
- 没有重试机制

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 切换到 PostgreSQL 或 MySQL
- 或实现重试逻辑 + WAL 模式

---

### 3.2 并发消息处理

**问题描述：**
- 没有任何乐观锁或悲观锁
- 时间戳依赖客户端，恶意用户可以伪造时间戳打乱消息顺序

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 服务端生成权威时间戳
- 或使用全局递增序列号

---

### 3.3 消息顺序保证

**问题描述：**
- 消息使用客户端时间戳排序
- 两个用户同时发送消息，时间戳可能相同，顺序不确定

**风险等级：中**

**现有缓解措施：** 无

---

### 3.4 离线消息的同步

**问题描述：**
- 离线用户的消息只保存在数据库
- 用户上线时没有"拉取离线消息"的主动推送机制
- 必须靠用户手动进入聊天页面才拉取历史

**风险等级：中**

**现有缓解措施：** 消息持久化（可拉取历史）

**建议修复方案：**
- 用户上线时主动推送离线消息

---

## 4. 认证与授权

### 4.1 Token 过期处理

**问题描述：**
- Token 过期时 `JWTError` 被捕获，返回 `None`
- 客户端收到通用错误，无法区分"过期"和"无效"

**风险等级：中**

**现有缓解措施：** Token 有过期时间

**建议修复方案：**
- 区分不同类型的 JWTError（过期 vs 签名无效），返回明确错误码

---

### 4.2 恶意用户模拟

**问题描述：**
- 如果 `SECRET_KEY` 泄露，攻击者可伪造任意用户 token
- 默认 secret key 是 `"your-secret-key-change-in-production"`

**风险等级：高**

**现有缓解措施：** Secret key 保密

**建议修复方案：**
- 强制要求配置环境变量
- 增加 token 刷新机制

---

### 4.3 权限提升攻击

**问题描述：**
- 客户端不能直接发送 `room_member_joined` 等系统消息（服务端不处理）
- 但服务端对房间操作的权限检查依赖数据库中的 `role` 字段

**风险等级：高**

**现有缓解措施：** WebSocket 认证

**建议修复方案：**
- 所有 room 操作必须经过服务端验证

---

### 4.4 未授权访问

**问题描述：**
- `admin.py` 所有接口（`/api/admin/*`）**完全没有授权检查**
- 任何人都可以调用 `GET /api/admin/stats`、`GET /api/admin/users` 等

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 添加管理员权限验证

---

## 5. 聊天室的特定问题

### 5.1 成员管理一致性

**问题描述：**
- 两套成员存储：
  - **数据库**：`room_members` 表
  - **内存**：`manager.room_members`（`connection.py`）
- `rooms.py` 用 `asyncio.create_task` 异步广播，不等待完成
- 如果广播前用户断线，内存状态与数据库不一致

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 统一以数据库为数据源，内存只做缓存
- 异步操作完成后验证一致性

---

### 5.2 消息广播丢失

**问题描述：**
- 广播时如果某个成员在过程中断线，消息丢失
- 没有广播完成确认

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 消息持久化到数据库，成员通过拉取获取未读消息
- 广播失败时标记消息为"未读"，成员上线时补发

---

### 5.3 房间销毁时的处理

**问题描述：**
- 删除房间后，只广播了 `room_deleted` 事件
- **没有清理 `manager.room_members`**，内存中的房间成员数据仍是旧数据

**风险等级：高**

**现有缓解措施：** 无

**建议修复方案：**
- 删除房间前先清理所有成员的内存状态

---

### 5.4 成员列表与消息推送的一致性

**问题描述：**
- 消息广播每次从数据库重新查询成员
- 但 `connection.py` 的 `room_members` 可能与数据库不同步
- 在线状态用 `manager.active_connections` 检查，但这是瞬时快照

**风险等级：中**

**现有缓解措施：** 每次从数据库查询

---

## 6. 服务端可用性

### 6.1 单点故障

**问题描述：**
- 所有连接和房间状态存储在**单进程内存**中
- 服务重启后所有内存数据丢失
- 用户需要重新认证和建立 WebSocket

**风险等级：高**

**现有缓解措施：** 消息持久化到数据库

**建议修复方案：**
- 使用 Redis 存储在线状态
- 实现会话恢复机制

---

### 6.2 数据库连接池

**问题描述：**
- 没有配置连接池大小，使用默认的 `pool_size=5`
- 高并发时连接可能耗尽

**风险等级：中**

**现有缓解措施：** 默认连接池

**建议修复方案：**
- 根据预期负载调整 `pool_size`
- 使用 `pool_pre_ping=True` 检测连接有效性

---

### 6.3 并发限制

**问题描述：**
- 使用默认的单进程模式
- 无 WebSocket 并发限制

**风险等级：中**

**现有缓解措施：** 无

**建议修复方案：**
- 使用 `uvicorn.run(app, workers=4)` 多进程

---

### 6.4 资源泄露

**问题描述：**
- SQLite 的 `check_same_thread=False` 允许跨线程使用连接，有线程安全问题
- 频繁创建新的 `SessionLocal()` 但没有统一管理

**风险等级：中**

**现有缓解措施：** 无

**建议修复方案：**
- 移除 `check_same_thread=False`
- 使用依赖注入的 session，确保正确释放

---

## 关键问题汇总

| 问题 | 风险等级 | 位置 |
|------|---------|------|
| 消息发送后接收者离线则丢失 | 高 | `websocket.py:114-122` |
| ACK 超时无处理 | 高 | `websocket.py:125-129` |
| SQLite 并发写入失败 | 高 | `database.py` |
| **Admin 接口无授权** | **高** | `admin.py` 全局 |
| 房间销毁不清理内存状态 | 高 | `rooms.py:161-162` |
| 广播丢失无补偿 | 高 | `websocket.py:202-204` |
| 多设备同时在线不支持 | 中 | `connection.py:9` |
| Ping 不验证 pong | 中 | `websocket.py:76-84` |
| Token 过期无明确错误 | 中 | `auth/utils.py:29-34` |
| 客户端时间戳可伪造 | 中 | `websocket.py:98` |
| 单点故障（内存状态） | 高 | `connection.py:113-114` |
| 数据库连接池未配置 | 中 | `database.py` |

---

## 优先修复建议

### P0（立即修复）
1. **Admin 接口添加授权** — 当前任何人可访问，必须立即修复
2. **消息持久化失败处理** — ACK 前应确保数据库写入成功

### P1（近期修复）
3. **房间销毁清理内存状态** — 删除房间时同步清理 `manager.room_members`
4. **添加消息状态（已送达/未送达）** — 支持离线消息补偿
5. **心跳超时验证** — 使用 `WS_PING_TIMEOUT` 断开无效连接

### P2（规划中）
6. 切换到 PostgreSQL/MySQL（解决 SQLite 并发问题）
7. 多设备同时在线支持
8. 会话恢复机制（解决单点故障）
9. 消息序列号（解决乱序问题）
